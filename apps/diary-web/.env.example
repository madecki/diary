# =============================================================================
# Diary Web (Next.js) — local development (copy to .env.local if needed)
# =============================================================================
NEXT_PUBLIC_DIARY_BASE_PATH=/mfe/diary
NEXT_PUBLIC_API_BASE_URL=http://localhost:4281

# Through gateway (localhost:3000) for HMR — set to "true" when using gateway dev
NEXT_PUBLIC_VIA_GATEWAY=false
